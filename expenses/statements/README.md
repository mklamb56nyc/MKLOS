# statements — processed archive

Statement files after ingestion, renamed `YYYY-MM-<account>.<ext>` (e.g.
`2026-06-sapphire.csv`). Source of truth for amounts and dates; every ledger row's
`source` column points back to a file here.
