# receipts — matched receipt files

Named `YYYY-MM-DD-<merchant>-<amount>.<ext>` (e.g. `2026-06-14-delta-412.30.pdf`).
Referenced from the ledger's `receipt` column. Receipts pulled from Gmail get saved
here too, noting the source message in the ledger `notes`.
